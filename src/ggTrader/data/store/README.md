# data/store

TimescaleDB read/write helpers shared across data sources and the feature
store. The existing `data/historical/timescaledb_loader.py` will migrate here
in **Phase 2** alongside feature-cache tables.
