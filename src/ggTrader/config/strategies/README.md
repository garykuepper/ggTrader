# config/strategies

YAML strategy definitions validated by Pydantic models on load. Populated
in **Phase 3+** alongside each new strategy. Format and schema are defined
when the first strategy lands.
